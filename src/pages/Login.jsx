import React from 'react';
import { Button } from 'react-bootstrap';
export default function Login() {
    return (
        <div>
            <h1>Login Page</h1>
            <Button>Login</Button>
        </div>
    );
}